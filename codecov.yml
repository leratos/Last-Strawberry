codecov:
  require_ci_to_pass: true

coverage:
  precision: 2
  round: down
  range: "40...100"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false

status:
  project:
    informational: false
    default:
      target: 0.45
      threshold: 1%
      if_not_found: failure
  patch:
    informational: false
    default:
      target: 0.70
      threshold: 1%
      if_not_found: failure
